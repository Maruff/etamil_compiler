{
  "name": "etamil-support",
  "displayName": "eTamil",
  "description": "Language support for eTamil, the Tamil-vocabulary FinTech DSL: syntax, completions, hover, and errors from the compiler itself.",
  "version": "0.5.0",
  "publisher": "etamil",
  "author": {
    "name": "Esan Maruff",
    "email": "esan@etamil.in"
  },
  "homepage": "https://etamil.in",
  "repository": {
    "type": "git",
    "url": "https://github.com/Maruff/etamil_compiler",
    "directory": "eTamil_Code"
  },
  "bugs": {
    "url": "https://github.com/Maruff/etamil_compiler/issues"
  },
  "//license": "AGPL-3.0-or-later, matching the repository's LICENSE. This said MIT until 0.3.0, which contradicted the compiler it ships alongside; the AGPL choice is deliberate and explained in the root README.",
  "license": "AGPL-3.0-or-later",
  "engines": {
    "vscode": "^1.84.0"
  },
  "categories": [
    "Programming Languages",
    "Snippets",
    "Linters"
  ],
  "keywords": [
    "Tamil",
    "eTamil",
    "FinTech",
    "DSL",
    "accounting",
    "தமிழ்"
  ],
  "//capabilities": "The extension runs a compiler binary and reads etamil.compilerPath, so it must not be active in a workspace the user has not trusted.",
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "eTamil runs the compiler to report errors, so it needs a trusted workspace."
    },
    "virtualWorkspaces": {
      "supported": "limited",
      "description": "Syntax, completions and hover work anywhere. Error checking needs the compiler on a real filesystem."
    }
  },
  "//activationEvents": "onLanguage only. The previous version also used onStartupFinished, so it activated in every window whether or not any eTamil file was open, and immediately offered to run an install command.",
  "activationEvents": [
    "onLanguage:etamil"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "etamil",
        "aliases": [
          "eTamil",
          "etamil"
        ],
        "extensions": [
          ".qmz"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "etamil",
        "scopeName": "source.etamil",
        "path": "./syntaxes/etamil.tmLanguage.json"
      }
    ],
    "snippets": [
      {
        "language": "etamil",
        "path": "./snippets/etamil.code-snippets"
      }
    ],
    "commands": [
      {
        "command": "etamil.run",
        "title": "Run this file",
        "category": "eTamil"
      },
      {
        "command": "etamil.serve",
        "title": "Serve this file (--async)",
        "category": "eTamil"
      },
      {
        "command": "etamil.install",
        "title": "Install the compiler for use outside the editor",
        "category": "eTamil"
      },
      {
        "command": "etamil.showOutput",
        "title": "Show the log",
        "category": "eTamil"
      }
    ],
    "menus": {
      "editor/title/run": [
        {
          "command": "etamil.run",
          "when": "resourceLangId == etamil",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "etamil.run",
          "when": "resourceLangId == etamil"
        },
        {
          "command": "etamil.serve",
          "when": "resourceLangId == etamil"
        }
      ]
    },
    "configuration": {
      "title": "eTamil",
      "properties": {
        "etamil.compilerPath": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "markdownDescription": "Path to the `etamil` binary. Empty means use the compiler carried in this extension, falling back to `etamil` on the `PATH` when this build carries none for your platform.\n\nSet it to run a compiler of your own — a `cargo build` of the repository, say. An explicit path always wins.\n\nMachine-scoped on purpose: this names an executable the extension runs, so a workspace must not be able to set it."
        },
        "etamil.eTamilFont": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "markdownDescription": "Font family for the ASCII that is **eTamil**, not English — `ceyal`, `moqqam`, the ezuqqu spellings in which one Latin letter stands for one Tamil letter. Empty, the default, draws everything in the editor font and changes nothing.\n\nThe editor keeps its ISO font as the base. Only eTamil-script ASCII is drawn in this face, so a name marked `_english`, a comment wrapped in `__ … __`, a string literal and the licence header all stay Latin. The rules are in `docs/reference/SCRIPT_RULES.md`.\n\nA CSS font stack: `eTamil Mono`, or `'eTamil Mono', monospace`. Machine-scoped, and checked before use, because the value is interpolated into the decoration's CSS."
        },
        "etamil.checkOnType": {
          "type": "boolean",
          "default": true,
          "description": "Report errors as you type, using `etamil --check`. That mode stops after the type checker, so your program is never run.",
          "scope": "resource"
        },
        "etamil.intelliSense": {
          "type": "boolean",
          "default": true,
          "description": "Offer completions and signature help for keywords, builtins and the nUlakam standard library.",
          "scope": "resource"
        }
      }
    },
    "configurationDefaults": {
      "[etamil]": {
        "editor.insertSpaces": true,
        "editor.tabSize": 4,
        "//comment": "Tamil renders poorly in most monospace faces, which have no Tamil glyphs at all and fall back mid-line. A stack that names Tamil faces first keeps a line of source visually even. It stays an ISO stack even for an author using the eTamil font: the base font is the one English has to be legible in, and etamil.eTamilFont paints the eTamil-script ASCII over the top of it.",
        "editor.fontFamily": "'Nirmala UI', 'Latha', 'Tamil Sangam MN', 'Noto Sans Tamil', Consolas, 'Courier New', monospace",
        "editor.wordBasedSuggestions": "currentDocument"
      }
    }
  },
  "scripts": {
    "generate": "python ../scripts/generate_editor_support.py",
    "check:generated": "python ../scripts/generate_editor_support.py --check",
    "build": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext .ts",
    "test": "node --test test/grammar.test.js test/snippets.test.js test/errors.test.js test/marks.test.js test/bundle.test.js",
    "verify": "npm run check:generated && npm run build && npm run lint && npm run test",
    "stage": "python ../scripts/package_extension.py --stage",
    "package": "vsce package",
    "package:all": "python ../scripts/package_extension.py",
    "vscode:prepublish": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.84.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "@vscode/vsce": "^2.32.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0",
    "vscode-oniguruma": "^2.0.1",
    "vscode-textmate": "^9.0.0"
  }
}
